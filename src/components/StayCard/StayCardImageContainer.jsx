import styled from 'styled-components'

const StayCardImageContainer = styled.div`
    & > img {
        border-radius: 24px;
    }

    border-radius: 24px;
    margin-block-end: 13.29px;
    overflow: hidden;
`

export {StayCardImageContainer}